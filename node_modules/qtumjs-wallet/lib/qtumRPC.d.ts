import RpcClient, { IConfig } from "qtumd-rpc";
import { Network } from "./Network";
export default class QtumRPC {
    rpc: RpcClient;
    constructor(config?: IConfig);
    generate(nblocks: number): Promise<any>;
}
export declare const rpcClient: QtumRPC;
export declare function generateBlock(network: Network): Promise<void>;
