

git filter-branch --commit-filter '
      if [ "$GIT_AUTHOR_EMAIL" = "elon@tesla.com" ];
      then
              GIT_AUTHOR_NAME="Howard Yeh";
              GIT_AUTHOR_EMAIL="hayeah@gmail.com";
              git commit-tree "$@";
      else
              git commit-tree "$@";
      fi' HEAD